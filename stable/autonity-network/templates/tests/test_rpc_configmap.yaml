apiVersion: v1
kind: ConfigMap
metadata:
  name: autonity-network-tests
data:
  run.sh: |-
    @test "Testing JSON-RPC of validator-0 is accessible" {
      curl --retry 1 --retry-delay 5 validator-0:8546
    }

    @test "Testing JSON-RPC of observer-0 is accessible" {
      curl --retry 1 --retry-delay 5 observer-0:8546
    }
